// Lib
import { Story } from '@storybook/react'

// Components
import Calendar from '@components/Calendar'

export default {
  title: 'Calendar',
  component: Calendar,
}

export const CalendarSample: Story = () => <Calendar />
